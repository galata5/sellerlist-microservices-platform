apiVersion: v1
kind: ConfigMap
metadata:
  name: payment-service-config
  labels:
    app: payment-service
data:
  SPRING_APPLICATION_NAME: PAYMENT-SERVICE
  SPRING_DATASOURCE_URL: jdbc:mysql://mysql.example.internal:3306/payment_service?useSSL=true&serverTimezone=UTC
  SPRING_DATASOURCE_USERNAME: payment_service
  INTERNAL_AUTH_EXPECTED_ISSUER: API-GATEWAY
  RABBITMQ_HOST: rabbitmq.example.internal
  RABBITMQ_PORT: "5672"
  RABBITMQ_USERNAME: payment_service
  SPRING_FLYWAY_ENABLED: "false"
---
apiVersion: v1
kind: Secret
metadata:
  name: payment-service-secret
  labels:
    app: payment-service
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: change-me
  INTERNAL_SERVICE_SECRET: change-me
  RABBITMQ_PASSWORD: change-me
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  labels:
    app: payment-service
    monitoring: enabled
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      port: 8400
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  labels:
    app: payment-service
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        monitoring: enabled
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: payment-service
          image: ghcr.io/your-org/payment-service:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8400
          envFrom:
            - configMapRef:
                name: payment-service-config
            - secretRef:
                name: payment-service-secret
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 800m
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /payment-service/actuator/health
              port: http
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /payment-service/actuator/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /payment-service/actuator/health
              port: http
            periodSeconds: 20
            timeoutSeconds: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: payment-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: payment-service
  labels:
    app: payment-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: payment-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payment-service
  labels:
    app: payment-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: payment-service
---
apiVersion: batch/v1
kind: Job
metadata:
  name: payment-service-migration
  labels:
    app: payment-service
spec:
  backoffLimit: 2
  template:
    metadata:
      labels:
        app: payment-service-migration
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: migrate
          image: ghcr.io/your-org/payment-service:v1.0.0
          imagePullPolicy: IfNotPresent
          command:
            - java
            - -jar
            - app.jar
            - --spring.main.web-application-type=none
          envFrom:
            - configMapRef:
                name: payment-service-config
            - secretRef:
                name: payment-service-secret
          env:
            - name: SPRING_FLYWAY_ENABLED
              value: "true"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
